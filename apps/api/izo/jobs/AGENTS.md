# Jobs · agent pointer

Наследует корневой `AGENTS.md` и `apps/api/AGENTS.md`.
Живая ownership-карта, invariants, owner-файлы и ближайшие tests находятся только в `README.md`.
Provider-specific lifecycle не дублировать здесь: использовать route `api.provider_execution`.
