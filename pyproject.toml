[tool.pytest.ini_options]
pythonpath = ["apps/api", "."]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = ["integration: disposable PostgreSQL/S3 integration only"]

[tool.ruff]
line-length = 100
target-version = "py313"
