# DOC-004 · Agent Development System — self-review

Base: `faec39d6ae0b4f035ef0f86114494789acde3b46` (`api/fal-klein-001`).
Рабочая ветка: `docs/agent-development-system`. Runtime product code не меняется.

## Что реализовано

- `PLAN.json`: один canonical lineage, один active и один next package; OpenRouter descendants fail-closed до reconciliation.
- `CURRENT.md`: короткая текущая точка без необходимости читать историю PR.
- `CONTEXT_MAP.json` + `tools/context.py`: 15 low-token routes для существующих web/backend/ops/docs областей.
- Стабильные `AGENTS.md`/INDEX больше не содержат текущий roadmap/SHA.
- Локальные ownership README добавлены для web Accounts/Admin/Credits/Gallery и backend Providers; Studio README сокращён до карты блока.
- `DOCS_SYSTEM.md`: владелец каждого класса фактов и progressive-disclosure правила.
- `DEVELOPMENT.md`: обязательный цикл LOCATE → SCOPE → IMPLEMENT → TARGETED TEST → SELF_REVIEW → CI/REVIEW.
- TASK/SELF_REVIEW/HANDOFF templates.
- `tools/check_docs.py` + `tests/test_docs_system.py` проверяют plan/current/context/local maps/stable docs/UTF-8.
- Старые STATUS/NEXT/DEVELOPMENT сохранены в `docs/history/`, а не удалены.
- ADR-001 фиксирует canonical provider lineage и запрещает выбор базы по номеру PR.

## SELF_REVIEW · найденные дефекты

1. Первая попытка изменить русский Markdown через PowerShell `Get-Content/Set-Content` создала mojibake в INDEX.
2. После добавления UTF-8 sanity расширенная проверка обнаружила тот же скрытый дефект в provider README.
3. Оба файла восстановлены безопасной UTF-8 записью; validator теперь сканирует все project Markdown кроме `.git/node_modules`.
4. SELF_REVIEW обнаружил, что корневой README всё ещё объявлял `image/server-workspace / PR #13` текущей линией и предлагал `git switch` на неё. README переписан как стабильный документ команд; прежняя версия архивирована.
5. Локальный Windows Python без UTF-8 mode ломает несколько существующих boundary-tests на чтении TSX через cp1251; это не regression DOC-004. Тот же набор с `PYTHONUTF8=1` проходит.
## Проверки до публикации

- `python tools/check_docs.py` — PASS; canonical/active/next и 15 routes согласованы.
- Context examples: gallery-download → `web.gallery`; fal cancel → `api.provider_execution` — PASS.
- `tests/test_docs_system.py` — 5/5 PASS.
- `tests/test_docs_system.py + test_change_scope.py + test_web_boundaries.py + test_image_boundaries.py`
  с `PYTHONUTF8=1` — 37/37 PASS.
- `git diff --check` — PASS.
- Machine scope — **32/32 PASS**, outside/sensitive violations 0.
- Полный GitHub CI exact head — PENDING на момент этой записи.

## Критический вывод

Решение не делает документацию «самообновляющейся»: semantic truth всё равно требует разработчика/reviewer.
Но теперь рассинхронизация становится fail-visible: две active-линии, missing context path, stale CURRENT marker,
mutable SHA в stable docs или mojibake дают автоматическую ошибку.

Context router намеренно не пытается понимать весь код. Он выбирает bounded starting set; correctness остаётся за
source/tests/self-review. Если будущая feature не имеет route/local map, сначала добавляется карта, а не выполняется широкий scan.

## Verdict

**SELF_REVIEW: PASS, remote CI pending.**

Следующее действие: закончить scope с этим review-файлом, commit/push DOC-004 и прочитать exact-head CI.
